//Complete this code or write your own from scratch
import java.util.*;
import java.io.*;
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        Map map=new LinkedHashMap();

        double n = in.nextInt();
        for(double i = 0; i < n; i++){
            String name = in.next();
            int phone = in.nextInt();     
            // Write code here
            map.put(name,phone);
        }
          ArrayList ss=new ArrayList();
        for(int i=0;i<n;i++){
         String name = in.next();
         ss.add(name);
        }
        Set set=map.keySet();
         Iterator it=set.iterator();
         while(it.hasNext()){
        boolean flag=false;
           String d=(String)it.next();
             for(int i=0;i<ss.size();i++){
                 //int iii=(int)i;
                if(d.equals(ss.get(i))){
                System.out.println(ss.get(i)+"="+map.get(d));
                flag=true;
                break;
                }
             }//for
             if(flag==false)
            System.out.println("Not found");
         }//while   
        in.close();
    }
}
//only first test case is working not others
